# 개발 계획

## 1. PySide6 기반 UI 프로토타입

- 기본 애플리케이션 창과 종목 카드 레이아웃을 구현한다.
- 카드 상단과 하단을 각각 1/3, 2/3 비율로 구성한다.
- 종목 코드, 현재가, ON/OFF 버튼 및 가격 영역의 기본 시각 요소를 배치한다.

## 2. 가짜 주가 데이터로 종목 카드 동작 테스트

- 실제 증권사 데이터 연동 전에 가짜 종목 및 주가 데이터를 준비한다.
- 일정 시간마다 현재가가 변하는 상황을 재현하여 카드 동작을 검증한다.

## 3. 현재가 기준 위/아래 가장 가까운 가격 계산

- 매입가, 반등가, 벽 데이터를 하나의 비교 가능한 가격 목록으로 관리한다.
- 현재가보다 높은 가격 중 가장 가까운 1개를 계산한다.
- 현재가보다 낮은 가격 중 가장 가까운 1개를 계산한다.
- 같은 가격이나 후보가 없는 경우의 처리 기준을 정한다.

## 4. 현재가 변화에 따른 UI 위치 갱신

- 현재가 변경 시 위쪽과 아래쪽 표시 대상을 다시 계산한다.
- 현재가와 표시 가격의 차이를 화면상의 거리로 변환한다.
- 급격한 이동을 줄이고 자연스럽게 보이도록 위치 갱신 방식을 조정한다.

## 5. 매입가 -5% 테두리 점멸

- OFF 상태에서 현재가가 매입가의 -5% 구간에 진입했는지 판정한다.
- 조건 충족 시 카드 테두리가 점멸하도록 구현한다.
- 구간을 벗어나면 점멸을 중지하고 기본 테두리로 복원한다.
- 소리 알림은 구현하지 않는다.

## 6. ON/OFF 상태 처리

- OFF를 미보유 또는 관심 종목 상태로 처리한다.
- ON을 현재 매수하여 보유 중인 상태로 처리한다.
- ON 상태에서는 매입가 접근 알림이 작동하지 않도록 한다.
- 상태 변경 시 카드 표시와 알림 조건을 즉시 갱신한다.

## 7. 화면 캡처

- Windows에서 마우스 포인터 오른쪽에 표시되는 영웅문 Global Tooltip 영역을 탐지하고 캡처한다.
- 화면 배율, 다중 모니터, 화면 경계에 가까운 Tooltip을 고려한다.

## 8. OCR

- 캡처된 Tooltip 이미지에서 텍스트와 숫자를 인식한다.
- 전처리와 OCR 설정을 조정하여 가격, 날짜, 거래량 등의 인식률을 개선한다.

## 9. 일봉/분봉 파서

- 일봉과 분봉 Tooltip의 서로 다른 크기와 항목 구성을 구분한다.
- OCR 텍스트를 각 형식에 맞는 구조화 데이터로 변환한다.
- 누락되거나 잘못 인식된 값에 대한 검증 규칙을 추가한다.

## 10. SQLite

- 종목 정보, 캡처 결과, 분석 결과 및 시간 정보를 저장할 스키마를 설계한다.
- 종목별 과거 분석 기록을 저장하고 조회할 수 있도록 구현한다.

## 11. AI 분석 모듈

- OCR 결과를 구조화하고 잘못 인식된 텍스트를 보정하는 분석 모듈을 구현한다.
- 규칙 기반 파싱 결과와 AI 결과를 비교하고 검증할 수 있도록 구성한다.

## 12. 테스트 및 README 정리

- 가격 선택 계산, 알림 조건, ON/OFF 상태, 파서 및 저장 기능을 테스트한다.
- Windows 설치 방법, Python 가상환경 구성, 실행 방법 및 사용법을 README에 정리한다.
- 알려진 제한 사항과 향후 개선 항목을 기록한다.
